import info.gridworld.actor.CrabCritter;
import java.util.ArrayList;

public class QuickCrab extends CrabCritter {

    /*moves to one of the 2 locations, randomly selected, taht are two spaces to its right or left, if that location & the intervening location are both empty.
      Otherwise, a QuickCrab moves like a CrabCritter */

    public ArrayList<Actors> getMoveLocations() {
    }
}
